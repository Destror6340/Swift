import UIKit

// 1)En un comentario o impresión define lo que es una constante, y en qué casos es utilizada.
print("Una constante se trata de un valor de algun dato que nuca se modifica, en swift se define con la palabra reservada 'let'\n")

// 2)En un comentario o impresión define lo que es una variable, y en qué casos es utilizada.

print("Una variable corresponde a datos que se modifican constantemente , como calculos de operaciones o contadores para iteraciones, en swift se define con la palabra reservada 'var'\n")

// 3)Al estar trabajando en la Nasa, te piden calcular la distancia a la que se encuentra cierto satélite utilizando el valor de pi. Almacena el valor de pi en la asociación que sea correcta de forma que optimicen memoria y usando la asociación creada, calcula la distancia a la que está de la tierra si la circunferencia que traza es de 12.5664m, imprime el resultado.s
let pi = 3.1416
let distance = 125664.0
var radio = distance/(2*pi)
print("radio: \(radio)\n")

// 4) En una empresa de seguros te piden almacenar la edad del cliente en un espacio en el que pueda ser modificada posteriormente. Utiliza la asociación adecuada para realizar dicha tarea y almacena tu edad en ella.
var edad = 35

// 5) Retira la función de comentario del apartado de la parte inferior, y corrige los nombres de las variables y constantes para hacer que corra nuevamente el programa.
var carro = "Run Run"
let Dress = "Jeans"
var valor_de_la_suma = 4+5
var funcc = true





